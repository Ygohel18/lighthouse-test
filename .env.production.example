# Production Environment Configuration
# Copy this file to .env.production and fill in your values

# ============================================
# MongoDB Configuration
# ============================================
MONGO_USERNAME=lighthouse_admin
MONGO_PASSWORD=CHANGE_THIS_STRONG_PASSWORD_123
MONGODB_URI=mongodb://lighthouse_admin:CHANGE_THIS_STRONG_PASSWORD_123@mongodb:27017/lighthouse?authSource=admin

# ============================================
# Redis Configuration
# ============================================
REDIS_PASSWORD=
# Leave empty if no password, or set a strong password

# ============================================
# MinIO/S3 Configuration
# ============================================
# If using Docker MinIO (in docker-compose.production.yml)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=CHANGE_THIS_MINIO_PASSWORD_456

# S3 Connection (use 'minio' as hostname if using Docker MinIO)
S3_ENDPOINT=http://minio:9000
# Or if using host MinIO: http://YOUR_VPS_IP:9000
# Or if using AWS S3: https://s3.amazonaws.com

S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=CHANGE_THIS_MINIO_PASSWORD_456
S3_BUCKET_NAME=lighthouse-screenshots
S3_REGION=us-east-1

# ============================================
# Application Configuration
# ============================================
NODE_ENV=production
LOG_LEVEL=info

# ============================================
# Security Notes
# ============================================
# 1. Generate strong passwords (at least 16 characters)
# 2. Use different passwords for each service
# 3. Never commit .env.production to git
# 4. Keep this file secure (chmod 600)
# 5. Rotate passwords regularly

# ============================================
# Quick Password Generator
# ============================================
# Run these commands to generate secure passwords:
# openssl rand -base64 32
# or
# pwgen -s 32 1
